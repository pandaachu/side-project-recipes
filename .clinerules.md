# Taiki Sato Portfolio - UI Design Guideline

> 日系極簡風格作品集網站設計指南
> 
> 適用於 Claude Code 切版使用

---

## 🎨 設計理念

- **極簡主義**: 去除不必要的裝飾，專注於內容呈現
- **功能與情緒的平衡**: 邏輯與直覺的完美結合
- **靜與動的融合**: 靜態內容與微動畫效果的和諧搭配
- **雙語設計**: 日文與英文並存，重視國際化體驗

---

## 🎨 色彩系統

### 主色調
```css
--color-primary: #000000;        /* 主要文字、邊框 */
--color-secondary: #FFFFFF;      /* 背景色 */
--color-accent: #333333;         /* 次要文字、hover 狀態 */
```

### 灰階系統
```css
--gray-50: #FAFAFA;
--gray-100: #F5F5F5;
--gray-200: #EEEEEE;
--gray-300: #E0E0E0;
--gray-400: #BDBDBD;
--gray-500: #9E9E9E;
--gray-600: #757575;
--gray-700: #616161;
--gray-800: #424242;
--gray-900: #212121;
```

### 使用規則
- 主要內容區域使用純白背景 (`#FFFFFF`)
- 文字使用純黑 (`#000000`)，確保最高對比度
- Hover 狀態使用 `#333333` 或添加 opacity
- 分隔線使用 `--gray-300` 或更淺的灰色

---

## 📝 字體系統

### 字體家族
```css
/* 日文優先 */
font-family: 'Hiragino Kaku Gothic ProN', 'Hiragino Sans', 
             'Yu Gothic', 'Meiryo', 
             -apple-system, BlinkMacSystemFont,
             'Segoe UI', sans-serif;

/* 英文標題 */
font-family: 'Helvetica Neue', 'Arial', sans-serif;
```

### 字體大小規範

```css
/* 標題層級 */
--font-size-h1: 48px;           /* Hero 大標題 */
--font-size-h2: 36px;           /* Section 標題 */
--font-size-h3: 24px;           /* 作品標題 */
--font-size-h4: 20px;           /* 子標題 */

/* 內文層級 */
--font-size-body-lg: 18px;      /* 大段落 */
--font-size-body: 16px;         /* 一般段落 */
--font-size-body-sm: 14px;      /* 小字、標籤 */
--font-size-caption: 12px;      /* 註解、版權 */

/* 裝飾文字 */
--font-size-display: 64px;      /* 漢字數字裝飾 */
```

### 字重系統
```css
--font-weight-light: 300;
--font-weight-regular: 400;
--font-weight-medium: 500;
--font-weight-bold: 700;
```

### 行高系統
```css
--line-height-tight: 1.2;       /* 標題 */
--line-height-normal: 1.6;      /* 一般段落 */
--line-height-relaxed: 1.8;     /* 長文閱讀 */
```

---

## 📏 間距系統

### 基礎間距單位
```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
--spacing-2xl: 48px;
--spacing-3xl: 64px;
--spacing-4xl: 96px;
--spacing-5xl: 128px;
```

### 容器寬度
```css
--container-max-width: 1200px;
--container-padding: 24px;      /* 手機 */
--container-padding-md: 48px;   /* 平板 */
--container-padding-lg: 64px;   /* 桌面 */
```

### Section 間距
```css
--section-spacing-sm: 60px;     /* 手機 */
--section-spacing-md: 100px;    /* 平板 */
--section-spacing-lg: 160px;    /* 桌面 */
```

---

## 🎯 元件設計規範

### 1. 導航列 (Navigation)

#### 桌面版
```css
.header {
  position: fixed;
  top: 0;
  width: 100%;
  height: 80px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--gray-200);
  z-index: 1000;
}

.nav-link {
  font-size: 14px;
  font-weight: 400;
  letter-spacing: 0.5px;
  color: var(--color-primary);
  text-decoration: none;
  transition: opacity 0.3s ease;
}

.nav-link:hover {
  opacity: 0.6;
}
```

#### 手機版 - 漢堡選單
```css
.menu-button {
  width: 40px;
  height: 40px;
  border: 1px solid var(--color-primary);
  background: transparent;
  cursor: pointer;
}

.mobile-menu {
  position: fixed;
  top: 0;
  right: -100%;
  width: 100%;
  height: 100vh;
  background: var(--color-secondary);
  transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.mobile-menu.open {
  right: 0;
}
```

### 2. Hero Section

```css
.hero {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  padding: 120px 24px 80px;
}

.hero-title {
  font-size: clamp(32px, 5vw, 64px);
  font-weight: 300;
  line-height: 1.3;
  letter-spacing: 0.02em;
  text-align: center;
  margin-bottom: 24px;
}

.hero-subtitle {
  font-size: clamp(14px, 2vw, 18px);
  font-weight: 400;
  letter-spacing: 0.1em;
  text-align: center;
  color: var(--gray-600);
}
```

### 3. 漢字數字裝飾

這是網站的特色設計元素：

```css
.number-decoration {
  font-size: 64px;
  font-weight: 300;
  color: var(--gray-300);
  position: absolute;
  top: -20px;
  left: -40px;
  z-index: -1;
  opacity: 0.3;
}

/* 使用範例: (一), (二), (三), (四), (五) */
```

### 4. 作品卡片 (Work Card)

```css
.work-card {
  position: relative;
  overflow: hidden;
  cursor: pointer;
  background: var(--color-secondary);
  border: 1px solid var(--gray-200);
  transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}

.work-card:hover {
  transform: translateY(-4px);
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.08);
  border-color: var(--color-primary);
}

.work-card-image {
  width: 100%;
  aspect-ratio: 16 / 10;
  object-fit: cover;
  transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}

.work-card:hover .work-card-image {
  transform: scale(1.05);
}

.work-card-content {
  padding: 24px;
}

.work-card-year {
  font-size: 12px;
  color: var(--gray-500);
  margin-bottom: 8px;
}

.work-card-title {
  font-size: 20px;
  font-weight: 500;
  margin-bottom: 16px;
  color: var(--color-primary);
}

.work-card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.work-card-tag {
  font-size: 11px;
  padding: 4px 12px;
  border: 1px solid var(--gray-300);
  color: var(--gray-600);
  letter-spacing: 0.5px;
}
```

### 5. 按鈕系統

#### Primary Button
```css
.btn-primary {
  padding: 16px 48px;
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 1px;
  color: var(--color-secondary);
  background: var(--color-primary);
  border: none;
  cursor: pointer;
  transition: all 0.3s ease;
  text-transform: uppercase;
}

.btn-primary:hover {
  background: var(--gray-800);
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
}
```

#### Outlined Button
```css
.btn-outlined {
  padding: 16px 48px;
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 1px;
  color: var(--color-primary);
  background: transparent;
  border: 1px solid var(--color-primary);
  cursor: pointer;
  transition: all 0.3s ease;
  text-transform: uppercase;
}

.btn-outlined:hover {
  color: var(--color-secondary);
  background: var(--color-primary);
}
```

#### Text Button (MORE/ALL WORKS)
```css
.btn-text {
  font-size: 14px;
  font-weight: 500;
  letter-spacing: 2px;
  color: var(--color-primary);
  background: transparent;
  border: none;
  border-bottom: 1px solid var(--color-primary);
  padding-bottom: 4px;
  cursor: pointer;
  transition: opacity 0.3s ease;
}

.btn-text:hover {
  opacity: 0.6;
}
```

### 6. 圖片畫廊 (Image Gallery)

```css
.gallery {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
  overflow: hidden;
}

.gallery-column {
  display: flex;
  flex-direction: column;
  gap: 16px;
  animation: scroll-vertical 20s linear infinite;
}

.gallery-column:nth-child(2) {
  animation-direction: reverse;
}

.gallery-item {
  width: 100%;
  height: auto;
  object-fit: cover;
  border-radius: 4px;
}

@keyframes scroll-vertical {
  0% { transform: translateY(0); }
  100% { transform: translateY(-50%); }
}
```

### 7. Footer

```css
.footer {
  padding: 80px 24px 40px;
  background: var(--gray-50);
  border-top: 1px solid var(--gray-200);
}

.footer-nav {
  display: flex;
  flex-wrap: wrap;
  gap: 32px;
  margin-bottom: 40px;
  justify-content: center;
}

.footer-link {
  font-size: 14px;
  color: var(--color-primary);
  text-decoration: none;
  transition: opacity 0.3s ease;
}

.footer-link:hover {
  opacity: 0.6;
}

.footer-social {
  display: flex;
  gap: 24px;
  justify-content: center;
  margin-bottom: 32px;
}

.footer-copyright {
  font-size: 12px;
  color: var(--gray-500);
  text-align: center;
}
```

---

## 🎬 動畫與互動

### 轉場時間
```css
--transition-fast: 0.2s;
--transition-normal: 0.3s;
--transition-slow: 0.4s;
--transition-slower: 0.6s;
```

### Easing 函數
```css
--ease-default: cubic-bezier(0.4, 0, 0.2, 1);
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

### 常用動畫效果

#### Fade In
```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.fade-in {
  animation: fadeIn 0.6s var(--ease-out) forwards;
}
```

#### Slide In
```css
@keyframes slideInLeft {
  from {
    opacity: 0;
    transform: translateX(-40px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
```

#### Hover Scale
```css
.hover-scale {
  transition: transform 0.4s var(--ease-default);
}

.hover-scale:hover {
  transform: scale(1.02);
}
```

---

## 📱 響應式設計

### Breakpoints
```css
/* 手機優先設計 */
--breakpoint-sm: 640px;   /* 大手機 */
--breakpoint-md: 768px;   /* 平板 */
--breakpoint-lg: 1024px;  /* 小桌面 */
--breakpoint-xl: 1280px;  /* 大桌面 */
--breakpoint-2xl: 1536px; /* 超大桌面 */
```

### 使用方式
```css
/* Mobile First */
.element {
  /* 手機樣式 */
}

@media (min-width: 768px) {
  .element {
    /* 平板樣式 */
  }
}

@media (min-width: 1024px) {
  .element {
    /* 桌面樣式 */
  }
}
```

### 響應式字體
```css
/* 使用 clamp() 實現流動字體 */
.responsive-heading {
  font-size: clamp(24px, 4vw, 48px);
}

.responsive-body {
  font-size: clamp(14px, 2vw, 16px);
}
```

### 容器查詢 (Container Queries)
```css
.card-container {
  container-type: inline-size;
}

@container (min-width: 400px) {
  .card {
    flex-direction: row;
  }
}
```

---

## 🎨 版面配置

### Grid 系統
```css
.grid-container {
  display: grid;
  grid-template-columns: repeat(12, 1fr);
  gap: 24px;
}

/* 作品列表 - 響應式 Grid */
.works-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 40px;
}

@media (min-width: 768px) {
  .works-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (min-width: 1024px) {
  .works-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}
```

### Flexbox 模式
```css
/* 水平置中 */
.flex-center {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 垂直排列 */
.flex-column {
  display: flex;
  flex-direction: column;
}

/* 兩端對齊 */
.flex-between {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
```

---

## ✨ 特殊效果

### 毛玻璃效果 (Glassmorphism)
```css
.glass {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}
```

### 漸層邊框
```css
.gradient-border {
  position: relative;
  border: 1px solid transparent;
  background: linear-gradient(white, white) padding-box,
              linear-gradient(45deg, #e0e0e0, #bdbdbd) border-box;
}
```

### 平滑滾動
```css
html {
  scroll-behavior: smooth;
}

/* 自訂滾動條 */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: var(--gray-100);
}

::-webkit-scrollbar-thumb {
  background: var(--gray-400);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--gray-500);
}
```

---

## 🌐 國際化設計

### 雙語排版
```html
<!-- 日文在上，英文在下 -->
<div class="bilingual-title">
  <span class="ja">私について</span>
  <span class="en">ABOUT</span>
</div>
```

```css
.bilingual-title {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.bilingual-title .ja {
  font-size: 24px;
  font-weight: 500;
}

.bilingual-title .en {
  font-size: 12px;
  font-weight: 400;
  letter-spacing: 2px;
  color: var(--gray-500);
}
```

---

## 🎯 無障礙設計 (Accessibility)

### 色彩對比
- 確保文字與背景對比度至少 4.5:1
- 大字體（18px+）對比度至少 3:1

### 鍵盤導航
```css
/* Focus 狀態 */
*:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* 取消 outline 時提供替代方案 */
.custom-focus:focus {
  outline: none;
  box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.2);
}
```

### 語意化 HTML
```html
<!-- 使用正確的標籤 -->
<nav aria-label="主要導航">
<main>
<article>
<section>
<header>
<footer>
```

### ARIA 標籤
```html
<button aria-label="開啟選單" aria-expanded="false">
  MENU
</button>

<img src="work.jpg" alt="作品標題 - 詳細描述">
```

---

## 📋 實作檢查清單

### 開發前確認
- [ ] 確認設計符合極簡主義原則
- [ ] 檢查色彩對比度
- [ ] 規劃響應式斷點
- [ ] 定義動畫效果

### 開發中確認
- [ ] 使用語意化 HTML
- [ ] 實作鍵盤導航
- [ ] 添加適當的 ARIA 標籤
- [ ] 測試不同螢幕尺寸
- [ ] 優化圖片載入
- [ ] 確保動畫流暢（60fps）

### 發布前確認
- [ ] 檢查無障礙性
- [ ] 測試跨瀏覽器相容性
- [ ] 優化效能（Core Web Vitals）
- [ ] 檢查 SEO 基礎設定
- [ ] 測試 Touch 裝置互動

---

## 🎨 設計原則總結

### Do ✅
- 保持極簡，專注內容
- 使用充足的留白
- 確保文字可讀性
- 提供流暢的動畫
- 重視細節質感
- 雙語並重
- 響應式優先

### Don't ❌
- 避免過度裝飾
- 不要使用過多顏色
- 避免複雜的排版
- 不要忽略手機體驗
- 避免過度動畫
- 不要犧牲可讀性

---

## 📦 推薦工具與資源

### 開發工具
- **Tailwind CSS**: 快速實作 utility-first CSS
- **Framer Motion**: React 動畫庫
- **GSAP**: 進階動畫效果
- **Next.js**: React 框架

### 設計工具
- **Figma**: 設計原型
- **Adobe XD**: UI 設計
- **Principle**: 動畫原型

### 測試工具
- **Lighthouse**: 效能與無障礙檢測
- **WebPageTest**: 載入效能分析
- **WAVE**: 無障礙檢測

---

## 📝 版本記錄

- **v1.0** (2025-01-29): 初始版本，基於 taikisato.com 分析建立

---

## 💡 使用建議

### 在 Claude Code 中使用
1. 將此文件放入專案根目錄
2. 在 `.clinerules` 中引用此設計指南
3. 切版時參考對應的元件規範
4. 遵循色彩、間距、字體系統

### 自訂調整
- 可根據專案需求調整間距系統
- 色彩可替換為品牌色
- 動畫時間可依據專案調整
- 響應式斷點可配合目標裝置

---

**設計理念**: 功能與情緒的平衡，邏輯與直覺的完美結合

**核心價值**: 極簡、優雅、專業、國際化